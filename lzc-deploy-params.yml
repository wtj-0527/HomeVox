params:
  - id: legacy_recovery_key
    type: secret
    optional: true
    name: "Legacy recovery key"
    description: "Operator-only key for one-time d53 legacy-project recovery. Leave this secret out of source control and ordinary logs."
