# Issue #19 Penpot design–implementation parity

Authoritative source read directly through the configured Streamable HTTP Penpot MCP on July 25, 2026: file `7622c4ac-2f6b-802b-8008-5f15321d47e8` (**HomeVox · 户型到可编辑 2D/3D**), page `7622c4ac-2f6b-802b-8008-5f15321d47e9` (**户型数字化主流程**). The six live 1440 × 960 boards are the source of truth; the deprecated file `7622c4ac-2f6b-802b-8008-5dd03ffef589` was not read or used.

Shared live facts: a 232px `#182033` rail; a 72px white top bar; Inter typography (20/700 title, 12/400 subtitle, 14/600 actions); `#f4f6fa` app background; 16px cards; 10px controls; violet `#5b5ce2`; success `#1f9d70`; and honest `#d98b21` `unknown` states. `productDesign.ts` and the production browser geometry assertion make those values a regression contract.

| Live Penpot board | Penpot fact → implemented product parity | Real behavior retained | Verification |
| --- | --- | --- | --- |
| 01 导入真实户型图 | Live board `d862747a-c9b2-80e7-8008-5f1b098c88cd`: 700px source card + 396px setup card at x=268/996; 16px cards; violet primary CTA; factual recognition rows. The product now keeps the selected image on Step 1, presents the two-card 1124px workspace, and uses the live copy including `unknown`. | Native browser image upload remains the entry point; **开始 AI 识别** invokes `/api/floorplans/parse`, rather than presenting a pre-filled result. | Fixed 1440 × 960 production screenshot `issue-19-import-ai.png`; layout has no horizontal overflow. The current live export SHA is `d71fcb3ff504c6d1b42464f581c404e205173ec9a098b2796e248f6c6e004c50`, which intentionally remains different from the supplied older frozen SHA `f536278e556d3d0056c4de745999baf4ee5f7dbb19f80aa1335afec511b92f7b`. |
| 02 AI 识别中 | Live board `d862747a-c9b2-80e7-8008-5f1b09a49b1e`: loading is a separate Step 2 and does not claim that unconfirmed values are facts. The product transitions only after the user starts the real parse and preserves its loading/error/retry feedback. | Parser failure remains actionable and does not unlock 2D. | Production parse-retry test and Step 2 flow guard. |
| 03 校正可编辑 2D | Live board `d862747a-c9b2-80e7-8008-5f1b09a9f669`: 870px workspace, 48px toolbar, 2D-first surface, right inspector, violet Undo/Redo and amber unconfirmed semantics. The product has that product toolbar and an accessible 2D editor/inspector shell without exposing raw IDs. | Canonical walls/openings, stable IDs (internal only), shared endpoint drag, opening edit, Undo/Redo, invalid-geometry fail-closed and 2D export remain real. | `issue-19-2d-correction.png`; production selects a wall/opening and verifies canonical edit/Undo/Redo. |
| 04 生成可编辑 3D | Live board `d862747a-c9b2-80e7-8008-5f1b09ae9891`: 812px preview card + 284px settings card; a 570px 3D surface; `同一空间数据`; unknown height/structure disclosures; violet confirm CTA. The product now mirrors that two-column composition. | It is a real R3F canvas fed by the Rust/WASM geometry revision, not an image; unavailable or invalid geometry stays fail-closed. | `issue-19-3d-confirm.png`; production asserts wall pixels, floor, openings, current renderer frame and canvas footprint. |
| 05 2D / 3D 联动编辑 | Live board `d862747a-c9b2-80e7-8008-5f1b09b35e19`: 1136px shared workspace, 48px toolbar, parallel 2D/3D, object state and explicit `unknown` non-upgrade copy. The product uses the same tri-panel workspace and customer-facing language. | 2D and 3D select the same canonical owner; edits regenerate the actual model; export refuses stale `toBlob` output. | `issue-19-linked-workspace.png`; production asserts selection before/after pixels, canonical revisions, opening ownership, edit/Undo/Redo, and stale-export rejection. |
| 06 保存为 HomeVox 项目 | Live board `d862747a-c9b2-80e7-8008-5f1b09b852b0`: centered 990px × 700px success card, 104px success circle, 28px title, save/continue state and `unknown` retained. The product save view follows that completion hierarchy rather than a diagnostic list. | Create/update/list/reload still use the real persisted document and source image. | Production lifecycle saves, restarts the Go process and reloads the same canonical document and geometry fingerprint. |

## Diagnostic and unknown-content policy

Raw canonical JSON and engineering telemetry (WASM, grid dimensions, triangle counts, timings and fallback labels) are absent from the ordinary DOM and accessibility tree. Actual invalid-geometry and unavailable-3D states remain fail-closed, actionable user messages. Unknown door/window dimensions are described as non-persistent previews rather than invented building facts.

## Exact-head evidence command

Run `npm --prefix frontend run test:e2e` on the PR exact head. The production Playwright suite performs controlled recognizable-fixture upload → OpenAI-compatible parse → canonical 2D selection → standalone 3D confirmation → linked selection/edit/Undo/Redo → Step 5 screenshot → manual project save → exact-PID restart → project reload. It captures the four named 1440 × 960 PNG artifacts, copies them outside Git into a unique run-artifact directory, emits `exact_head` and `artifact_path`, verifies all four images are present and distinct, and covers invalid/duplicate canonical identity, completion-gated navigation, unavailable WASM, and stale-3D-export fail-closed behavior.
