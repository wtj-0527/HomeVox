# Issue #19 Penpot design–implementation parity

Authoritative source read directly through the configured Streamable HTTP Penpot MCP on July 25, 2026: file `7622c4ac-2f6b-802b-8008-5f15321d47e8` (**HomeVox · 户型到可编辑 2D/3D**), page `7622c4ac-2f6b-802b-8008-5f15321d47e9` (**户型数字化主流程**). The six live 1440 × 960 boards are the source of truth; the deprecated file `7622c4ac-2f6b-802b-8008-5dd03ffef589` was not read or used.

Shared live facts: a 232px `#182033` rail; a 72px white top bar; Inter typography (20/700 title, 12/400 subtitle, 14/600 actions); `#f4f6fa` app background; 16px cards; 10px controls; violet `#5b5ce2`; success `#1f9d70`; and amber `#d98b21` for values that still need confirmation. `productDesign.ts`, `productPresentation.ts`, and the production browser geometry assertions are regression contracts.

| Live Penpot board | Penpot fact | Current implementation / deliberate difference | Acceptance |
| --- | --- | --- | --- |
| 01 导入真实户型图 | Board `d862747a-c9b2-80e7-8008-5f1b098c88cd`: x=268/996 cards (700/396), y=112, h=780; source image x=318 y=188 600×560; one primary and one reselect control. | Exact card/image geometry is browser-gated. The repeated reselect entry was removed. Live prototype’s literal `unknown` is shown to customers as **尚未识别** / **尚未测量**, preserving the underlying state without leaking implementation vocabulary. | Fixed 1440×960 DOM boxes, no overflow, one reselect entry, real browser upload and `/api/floorplans/parse`. The current live export SHA remains `d71fcb3ff504c6d1b42464f581c404e205173ec9a098b2796e248f6c6e004c50`, different from supplied frozen SHA `f536278e556d3d0056c4de745999baf4ee5f7dbb19f80aa1335afec511b92f7b`. |
| 02 AI 识别中 | Board `d862747a-c9b2-80e7-8008-5f1b09a49b1e`: recognition is a distinct, honest in-progress state. | Real parse starts only from the Step-1 CTA; failure copy is actionable and customer-facing rather than a transport/status-code dump. | Retry remains gated; a failed parse cannot unlock editing. |
| 03 校正可编辑 2D | Board `d862747a-c9b2-80e7-8008-5f1b09a9f669`: x=256 y=92 870×800 editor, x=1146 246×800 inspector, 48px toolbar, light 2D surface and 64px source controls. | Adaptive `xMidYMid meet` SVG removes the prior right-edge black crop, centers the plan and keeps pointer hit targets stable. Labels, object state and history actions are Chinese; durable IDs remain internal. | Fixed 1440×960 DOM boxes; selection, drag, opening edit, undo/redo, invalid-geometry close and export all remain production-tested. |
| 04 生成可编辑 3D | Board `d862747a-c9b2-80e7-8008-5f1b09ae9891`: x=268/1108 cards (812/284), y=112, h=780 and 570px preview surface. | The title is stateful: **空间预览已准备好** only after the real visible canvas is admitted; otherwise it says preparation is in progress. Prototype literal `unknown` becomes **尚未测量** / **待确认** for customers. | Production verifies the live canvas footprint, wall/floor/opening pixels, renderer admission and no overlay obscures the model. |
| 05 2D / 3D 联动编辑 | Board `d862747a-c9b2-80e7-8008-5f1b09b35e19`: x=256 y=92 1136×800 shell and 48px toolbar. | The implementation deliberately retains a third inspector column because the real edit/export controls must remain reachable while both live views stay visible; this is the only structural deviation from Penpot’s bottom summary strip. Each panel fills the available height, avoiding the prior empty lower 2D area. | Production checks 2D/3D selection, customer-facing visible copy, canvas pixels, opening voids, bounded before/after selection change, edit/undo/redo regeneration and stale-export rejection. |
| 06 保存为 HomeVox 项目 | Live board `d862747a-c9b2-80e7-8008-5f1b09b852b0`: centered 990px × 700px success card, 104px success circle, 28px title, save/continue state and `unknown` retained. The product save view follows that completion hierarchy rather than a diagnostic list. | Create/update/list/reload still use the real persisted document and source image. | Production lifecycle saves, restarts the Go process and reloads the same canonical document and geometry fingerprint. |

## Diagnostic and unknown-content policy

Internal identities and rendering telemetry are absent from the ordinary DOM and accessibility tree. Actual invalid-geometry and unavailable-3D states remain fail-closed with actionable customer messages. Values that have not been measured or recognized are presented as **待确认**, **尚未测量**, or **尚未识别**; no value is invented.

## Exact-head evidence command

Run `npm --prefix frontend run test:e2e` on the PR exact head. The production Playwright suite performs controlled recognizable-fixture upload → OpenAI-compatible parse → canonical 2D selection → standalone 3D confirmation → linked selection/edit/Undo/Redo → Step 5 screenshot → manual project save → exact-PID restart → project reload. It captures the four named 1440 × 960 PNG artifacts, copies them outside Git into a unique run-artifact directory, emits `exact_head` and `artifact_path`, verifies all four images are present and distinct, and covers invalid/duplicate canonical identity, completion-gated navigation, unavailable WASM, and stale-3D-export fail-closed behavior.
